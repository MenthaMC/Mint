From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Bacteriawa <A3167717663@hotmail.com>
Date: Mon, 6 Oct 2025 23:49:45 +0800
Subject: [PATCH] Add Networkbar 2


diff --git a/net/minecraft/network/Connection.java b/net/minecraft/network/Connection.java
index 789ba719b3b4ee296e6b93a34702fc6b1323f8b1..30cb8f6fad83bae3ec5447d207f0d8518da74e99 100644
--- a/net/minecraft/network/Connection.java
+++ b/net/minecraft/network/Connection.java
@@ -66,8 +66,6 @@ import org.slf4j.Logger;
 import org.slf4j.Marker;
 import org.slf4j.MarkerFactory;
 
-import static dev.bacteriawa.mint.functions.GlobalServerNetworkBar.updatePlayerNetworkData;
-
 public class Connection extends SimpleChannelInboundHandler<Packet<?>> {
     private static final float AVERAGE_PACKETS_SMOOTHING = 0.75F;
     private static final Logger LOGGER = LogUtils.getLogger();
@@ -333,13 +331,6 @@ public class Connection extends SimpleChannelInboundHandler<Packet<?>> {
                 // Paper end - packet limiter
                 if (packetListener.shouldHandleMessage(packet)) {
                     try {
-                        // Mint start - Update player network data for received packets
-                        net.minecraft.server.level.ServerPlayer player = this.getPlayer();
-                        if (player != null) {
-                            updatePlayerNetworkData(player.getBukkitEntity(), packet.toString().length(), 0, 1, 0);
-                        }
-                        // Mint end - Update player network data for received packets
-                        
                         genericsFtw(packet, packetListener);
                     } catch (RunningOnDifferentThreadException var5) {
                     } catch (io.papermc.paper.util.ServerStopRejectedExecutionException ignored) { // Paper - do not prematurely disconnect players on stop
@@ -535,12 +526,6 @@ public class Connection extends SimpleChannelInboundHandler<Packet<?>> {
             return;
         }
         try {
-        // Mint start - Update player network data for sent packets
-        if (player != null) {
-            updatePlayerNetworkData(player.getBukkitEntity(), 0, packet.toString().length(), 0, 1);
-        }
-        // Mint end - Update player network data for sent packets
-
         final ChannelFuture channelFuture;
         // Paper end - Optimize network
         if (channelFutureListener != null) {
diff --git a/net/minecraft/network/PacketDecoder.java b/net/minecraft/network/PacketDecoder.java
index 8b1bd09af525fe58d69a55185cf4a356af00d8a4..1316c9d2bce3cf11d4dbc2608ba30bb6c795af28 100644
--- a/net/minecraft/network/PacketDecoder.java
+++ b/net/minecraft/network/PacketDecoder.java
@@ -2,6 +2,7 @@ package net.minecraft.network;
 
 import com.mojang.logging.LogUtils;
 import dev.bacteriawa.mint.functions.NetworkAnalyser;
+import dev.bacteriawa.mint.functions.GlobalServerNetworkBar;
 import io.netty.buffer.ByteBuf;
 import io.netty.channel.ChannelHandlerContext;
 import io.netty.handler.codec.ByteToMessageDecoder;
@@ -39,15 +40,28 @@ public class PacketDecoder<T extends PacketListener> extends ByteToMessageDecode
             JvmProfiler.INSTANCE.onPacketReceived(this.protocolInfo.id(), packetType, context.channel().remoteAddress(), i);
             
             // Mint start - Track received packets with accurate byte size
-            if (NetworkAnalyser.isRunning()) {
-                try {
-                    Object handler = context.pipeline().get("packet_handler");
-                    if (handler instanceof Connection) {
-                        NetworkAnalyser.onPacketReceived((Connection) handler, packet.getClass().getSimpleName(), i);
+            try {
+                Object handler = context.pipeline().get("packet_handler");
+                if (handler instanceof Connection) {
+                    Connection connection = (Connection) handler;
+                    
+                    // NetworkAnalyser tracking
+                    if (NetworkAnalyser.isRunning()) {
+                        NetworkAnalyser.onPacketReceived(connection, packet.getClass().getSimpleName(), i);
+                    }
+                    
+                    // GlobalServerNetworkBar update - track all connections, not just game players
+                    net.minecraft.server.level.ServerPlayer player = connection.getPlayer();
+                    if (player != null) {
+                        // Game phase - track per player
+                        GlobalServerNetworkBar.updatePlayerNetworkData(player.getBukkitEntity(), i, 0, 1, 0);
+                    } else {
+                        // Non-game phase (login, config, etc.) - track globally
+                        GlobalServerNetworkBar.updateGlobalNetworkData((long)i, 0L, 1L, 0L);
                     }
-                } catch (Exception e) {
-                    // Silently ignore to prevent packet transmission issues
                 }
+            } catch (Exception e) {
+                // Silently ignore to prevent packet transmission issues
             }
             // Mint end
             
diff --git a/net/minecraft/network/PacketEncoder.java b/net/minecraft/network/PacketEncoder.java
index 2e67d8337a80b152967883637520d3c0542a32c5..0d89c7db69f1eb481a3ed2d71c0df8a45a363650 100644
--- a/net/minecraft/network/PacketEncoder.java
+++ b/net/minecraft/network/PacketEncoder.java
@@ -2,6 +2,7 @@ package net.minecraft.network;
 
 import com.mojang.logging.LogUtils;
 import dev.bacteriawa.mint.functions.NetworkAnalyser;
+import dev.bacteriawa.mint.functions.GlobalServerNetworkBar;
 import io.netty.buffer.ByteBuf;
 import io.netty.channel.ChannelHandlerContext;
 import io.netty.handler.codec.MessageToByteEncoder;
@@ -36,15 +37,28 @@ public class PacketEncoder<T extends PacketListener> extends MessageToByteEncode
             JvmProfiler.INSTANCE.onPacketSent(this.protocolInfo.id(), packetType, channelHandlerContext.channel().remoteAddress(), i);
             
             // Mint start - Track sent packets with accurate byte size
-            if (NetworkAnalyser.isRunning()) {
-                try {
-                    Object handler = channelHandlerContext.pipeline().get("packet_handler");
-                    if (handler instanceof Connection) {
-                        NetworkAnalyser.onPacketSent((Connection) handler, packet.getClass().getSimpleName(), i);
+            try {
+                Object handler = channelHandlerContext.pipeline().get("packet_handler");
+                if (handler instanceof Connection) {
+                    Connection connection = (Connection) handler;
+                    
+                    // NetworkAnalyser tracking
+                    if (NetworkAnalyser.isRunning()) {
+                        NetworkAnalyser.onPacketSent(connection, packet.getClass().getSimpleName(), i);
+                    }
+                    
+                    // GlobalServerNetworkBar update - track all connections, not just game players
+                    net.minecraft.server.level.ServerPlayer player = connection.getPlayer();
+                    if (player != null) {
+                        // Game phase - track per player
+                        GlobalServerNetworkBar.updatePlayerNetworkData(player.getBukkitEntity(), 0, i, 0, 1);
+                    } else {
+                        // Non-game phase (login, config, etc.) - track globally
+                        GlobalServerNetworkBar.updateGlobalNetworkData(0L, (long)i, 0L, 1L);
                     }
-                } catch (Exception e) {
-                    // Silently ignore to prevent packet transmission issues
                 }
+            } catch (Exception e) {
+                // Silently ignore to prevent packet transmission issues
             }
             // Mint end
         } catch (Throwable var9) {
